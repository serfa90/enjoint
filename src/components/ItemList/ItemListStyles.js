export const ItemListStyles = theme => {
    return({
        root: {
            display: 'flex',
            width: '100%',
            justifyContent: 'center',
        },
        contPadre: {
            padding: theme.spacing(2),
        }
    })
}